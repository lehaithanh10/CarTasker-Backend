name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  ci:
    uses: lehaithanh10/cartaskers-infra/.github/workflows/ci-node.yml@v1.0.0
    with:
      working-directory: "."
      node-version: "20"
      run-tests: true
      run-build: true
